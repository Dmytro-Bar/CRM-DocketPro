# DocketPro CRM — Інструкція користувача

> Актуальна версія: 1.2 · Оновлено: 28.05.2026

---

## 🏠 Дашборд (`/`)

Головна сторінка з оглядом поточного стану бізнесу.

**Фільтри вгорі:** оберіть період (місяць / тиждень / квартал / рік), клієнта або тип договору — всі KPI перераховуються.

**KPI-картки:**
- **Виставлено** — загальна сума рахунків за період
- **Оплачено** — фактично сплачено
- **Борг** — прострочено + в строку (не оплачено)
- **Витрати** — сума витрат за період
- **ROI** — (дохід − витрати) / витрати × 100%

**Блок курсу НБУ** — відображається якщо є договори з прив'язкою до курсу. Показує поточний курс USD/UAH та таблицю договорів з відхиленням (%). Рядки з перевищенням порогу підсвічені жовтим.

**Графік** — 6 місяців: дохід (синій) vs витрати (червоний).

**Таблиці внизу:**
- Рахунки зі статусами (прострочені — вгорі, червоні)
- Рахунки без актів
- Акти що потребують підписання
- Договори що закінчуються (≤60 днів)
- Наступні рахунки до виставлення

---

## 👥 Клієнти (`/clients`)

**Список:** пошук по назві або ЄДРПОУ. Колонки сортуються кліком на заголовок.

**Додати клієнта:** кнопка "Новий клієнт" → форма з полями: назва, ЄДРПОУ, директор, email, телефон, адреса, статус.

**Редагувати / видалити:** іконки в рядку таблиці. ⚠️ Видалення клієнта каскадно видаляє всі його договори, рахунки та акти.

**Excel-експорт:** кнопка "Excel" вгорі.

---

## 📄 Договори (`/contracts`)

**Фільтри:** вкладки статусу (Всі / Активний / Призупинено / Завершено / Чернетка / Закінчуються), пошуковий рядок, тип (Доступ / Погодинний). Перемикач Таблиця / Картки.

**Заголовок сторінки** показує: кількість активних, MRR, ARR, борг.

**Клік на рядок** → drawer з деталями договору:
- Умови, тариф, сума/міс, строк дії
- Нотатки та скан договору
- Статистика рахунків (всього / несплачено / актів)
- **Додаткові угоди (ДУ)** — історія змін умов

### Додаткові угоди (ДУ)

У drawer договору знизу є секція "Додаткові угоди". Кожна зміна умов договору (кількість користувачів, тариф, строк) фіксується як окрема ДУ зі збереженням історії.

**Додати ДУ:** натисни "+ Нова ДУ" →
- **№ ДУ** (обов'язково): напр. "ДУ №1", "ДУ-2"
- **Дата підписання** та **Набирає чинності** (дд.мм.рррр)
- **Що змінилось:** кількість користувачів / тариф / строк — залиш порожнім якщо не змінилось
- **Примітки** — опис змін довільним текстом
- **Скан ДУ** — PDF або JPG підписаного документа
- **"Оновити умови договору одразу"** — якщо ДУ вже набрала чинності, поля договору оновляться автоматично; якщо дата в майбутньому — зміни зафіксуються в історії без оновлення

**Видалити ДУ:** іконка кошика в картці ДУ (також видаляє файл скану).

### Прив'язка до курсу НБУ

У формі договору (розділ "Прив'язка до курсу НБУ"):
- Встанови прапорець "Відстежувати курс НБУ"
- Зафіксуй базовий курс USD/UAH на дату укладення (або натисни "НБУ" для автозаповнення)
- Вкажи поріг відхилення % (за замовчуванням 5%)

При перевищенні порогу — попередження на дашборді з таблицею договорів та % відхилення. Курс оновлюється автоматично раз на добу.

---

## 🧾 Рахунки (`/invoices`)

**Фільтри:** статус, клієнт, рядковий пошук, рік. Підсумки вгорі: оплачено / прострочено / не оплачено / всього.

**Виставити рахунок:** кнопка "Новий рахунок":
- Оберіть договір → тип (Доступ або Погодинний) підтягується автоматично
- **Доступ:** виберіть період (кнопки 1/2/3/6/12 міс або вручну), сума розраховується автоматично (тариф × кор-чі × місяці)
- **Погодинний:** вкажіть години, сума = години × ставка
- За потреби вкажіть знижку % та дату рахунку (backdating)
- Система генерує Word → конвертує в PDF → зберігає у `Договори/{клієнт}/...`

**Дії в рядку:**
- 💰 **Оплачено** — відмітити як оплачений (вказати дату)
- ✕ **Скасувати** — скасувати рахунок
- ✉ **Нагадування** — генерує лист-нагадування в PDF

**Клік на рядок** → drawer з деталями: усі поля, PDF-посилання, кнопки дій.

**Excel-експорт** — поточний відфільтрований список.

---

## ✅ Акти (`/acts`)

**Виставити акт:** кнопка "Новий акт" → обираєш рахунок без акту → система підтягує дані та генерує Word/PDF за шаблоном.

**Кастомний шаблон:** якщо у договорі вказано поле `act_template` — використовується той шаблон; інакше стандартний (Доступ або Погодинний).

**Статуси:**
- **Чернетка** → **Надіслано** → **Підписано**
- Зміна статусу — через кнопки у рядку або drawer

**Excel-експорт** доступний.

---

## 🏦 Банк (`/payments`)

Звірка надходжень з банківською випискою.

**Завантаження виписки:** кнопка "Завантажити CSV" — підтримується формат Приват24.

**Monobank:** якщо вказаний `MONO_TOKEN` у `.env` — натисни "Завантажити з Monobank" для отримання транзакцій.

**Алгоритм зіставлення** (автоматично):
1. Точний збіг по номеру рахунку в призначенні платежу
2. Назва клієнта (нечіткий пошук)
3. ЄДРПОУ у призначенні
4. LiqPay — автоматично зберігається в Оплати

**Дії:** клікни на транзакцію → відмітити відповідний рахунок як оплачений.

---

## 💳 Оплати (`/app-payments`)

Позадоговірні надходження — LiqPay, готівка, банк, інше.

**Додати:** кнопка "+ Оплата" → дата, сума, джерело (LiqPay / Приват24 / Банк / Готівка / Інше), опис.

**Фільтри:** дата від/до, джерело.

Ці надходження враховуються у KPI дашборду та у розрахунку податків.

---

## 💚 LiqPay (`/lyqpay`)

Перегляд платежів отриманих через LiqPay. Дані підтягуються з Monobank API (транзакції з ознакою liqpay).

Таблиця сортується по всіх колонках. Платежі що не зіставлені з рахунками — відображаються окремо.

---

## 💸 Витрати (`/expenses`)

**Список:** фільтри за датою та категорією. Колонки сортуються. Сума відображається в гривнях + підрядок з оригінальною валютою та курсом (якщо не UAH).

**Додати витрату:** форма зліва:
- Дата, категорія, опис
- Сума + валюта (UAH / USD / EUR)
- Якщо не UAH — вкажи курс, сума в грн розраховується автоматично

**Редагувати:** клікни на рядок → відкривається drawer з live-перерахунком суми в грн.

**Категорії:** кнопка "Категорії" (шестерня) → панель де можна додати або видалити категорії.

**Excel-експорт** доступний.

---

## 📊 Податки (`/taxes`)

Звіт для ФОП 3-ї групи: єдиний податок (ЄП) 5% та військовий збір (ВЗ) 1%.

**Вкладки по роках** — клік перемикає рік.

**Таблиця звітних періодів** (наростаючим з початку року):

| Період | Дохід кварт. | Наростаючим | ЄП 5% | ВЗ 1% | Разом |
|---|---|---|---|---|---|
| I квартал | ... | ... | ... | ... | **→ в декларацію** |
| Півріччя | ... | ... | ... | ... | **→ в декларацію** |
| 9 місяців | ... | ... | ... | ... | **→ в декларацію** |
| Рік | ... | ... | ... | ... | **→ в декларацію** |

Колонка **"Разом (наростаючим)"** — це саме та сума, яку вносиш у декларацію за відповідний звітний період.

**Джерела доходу:**
- Оплачені рахунки (статус "Оплачено", береться дата оплати)
- Записи в "Оплати" (всі джерела)
- LiqPay-платежі автоматично перераховуються на валову суму (+1,5% комісії)

> **Важливо про LiqPay:** на рахунок надходить сума за мінусом 1,5% комісії. Для цілей оподаткування система автоматично додає комісію назад. Наприклад: надійшло ₴3 181,55 → дохід для декларації ₴3 230,00.

---

## 📋 Шаблони (`/templates`)

Управління Word-шаблонами для генерації документів.

**Список шаблонів:** класифікація за типом (Рахунок / Акт / Лист). Показує які договори використовують кожен шаблон.

**Завантажити шаблон:** кнопка "Завантажити" → обери файл `.docx` та категорію.

**Редактор у браузері:** кнопка "Редагувати" → текстовий редактор з A4-макетом та панеллю змінних. Клік на змінну вставляє `{{ЗМІННА}}` у текст.

**Доступні змінні:**

| Група | Змінні |
|---|---|
| Документ | `{{INVOICE_NO}}`, `{{ACT_NO}}`, `{{CONTRACT_NO}}`, `{{INVOICE_DATE}}`, `{{ACT_DATE}}` |
| Клієнт | `{{CLIENT_NAME}}`, `{{CLIENT_CODE}}`, `{{CLIENT_DIRECTOR}}`, `{{CLIENT_ADDRESS}}` |
| Дати | `{{PERIOD_FROM}}`, `{{PERIOD_TO}}`, `{{DUE_DATE}}`, `{{DUE_LINE}}`, `{{TODAY_DATE}}` |
| Послуги | `{{USERS}}`, `{{MONTHS}}`, `{{HOURS}}`, `{{HOUR_RATE}}`, `{{TARIFF}}`, `{{SERVICE_SUBJECT}}` |
| Сума | `{{SUM}}`, `{{SUM_WORDS}}`, `{{DISCOUNT_LINE}}` |

**Призначити до договору:** у drawer шаблону → "Призначити" → обери договір.

---

## ⚙️ Загальні можливості

**Сортування таблиць** — клік на заголовок колонки → сортування ↑, повторний клік → ↓. Підтримується на всіх сторінках.

**Drawer** — клік на рядок таблиці відкриває бокову панель з деталями. Закрити — кнопка ✕ або клік поза панеллю.

**Excel-експорт** — доступний на сторінках: Клієнти, Договори, Рахунки, Акти, Витрати.

**Запуск сервера:**
```bash
cd /Users/dmytrobarabin/Documents/03_DOCKETPRO/CRM/_web
source ../venv/bin/activate
uvicorn main:app --reload --port 8000
```
Відкрити: **http://localhost:8000**

Якщо порт зайнятий:
```bash
lsof -ti :8000 | xargs kill -9
```

---

*Інструкція оновлюється автоматично при додаванні нових функцій.*
